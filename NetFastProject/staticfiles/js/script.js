// Placeholder script for register page
// Left intentionally empty so missing file won't produce 404/JS errors.
(function(){
    // no-op
    if (typeof window !== 'undefined' && window.console) {
        console.debug('script.js loaded (placeholder)');
    }
})();